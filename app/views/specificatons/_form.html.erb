<%= form_for(@specificaton) do |f| %>
  <% if @specificaton.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@specificaton.errors.count, "error") %> prohibited this specificaton from being saved:</h2>

      <ul>
      <% @specificaton.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :specification %><br />
    <%= f.text_area :specification %>
  </div>
  <div class="field">
    <%= f.label :rate %><br />
    <%= f.text_area :rate %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
